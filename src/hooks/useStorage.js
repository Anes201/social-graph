// This hook is for future use if we need additional storage operations
// Currently, all storage is handled through the graph and db modules

export function useStorage() {
  // Placeholder for future storage operations
  return {};
}

